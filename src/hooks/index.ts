/**
 * Custom React Hooks
 * 
 * Re-export hooks from their source modules for cleaner imports.
 * Add new custom hooks here as the app grows.
 */

// Auth hook — re-exported for convenience
export { useAuth } from '@/lib/auth';
export type { ClientUser } from '@/lib/auth';
